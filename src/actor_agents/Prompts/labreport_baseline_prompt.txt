### Instructions:
You are a data extraction tool for scientific laboratory reports.
Extract ALL data from this report and return it as a structured JSON object.

CORE RULES:
1. TABLE DETECTION:
   If NO table is present on this page, return ONLY: {"no_table": true}

2. TABLE EXTRACTION:
   • Extract every table as a JSON array of objects (one object per row).
   • Use EXACT column header names as JSON keys — preserve case, units, symbols.
   • Extract ALL rows in EXACT document order — do not skip any row.
   • Preserve cell values verbatim:
       numbers       → exact as written (e.g. 10.52, 0.003)
       detection lim → <0.01  ND  BDL  <LOD  <LOQ
       uncertainty   → 10.5 ± 0.3
       status        → "Pass"  "FAIL"  "Compliant"
       empty cell    → null

3. MULTIPLE TABLES:
   Use the table's title as its JSON key, or "Table_1", "Table_2", etc.

4. FIGURES AND GRAPHS:
   Add each figure to a "figures" array:
   { "figure_id": "...", "title": "...", "x_axis": "...", "y_axis": "...",
     "series": [...], "equation": "...", "r_squared": "...", "description": "..." }
   Set missing fields to null.

5. DOCUMENT METADATA:
   Extract all identification fields (Report No., Date, Sample ID, Analyst,
   Test Method, Laboratory, Accreditation, etc.) as top-level key-value pairs.

6. RESULTS / CONCLUSIONS:
   If a narrative section exists, extract:
     "results_summary", "regulatory_limits", "compliance_status", "remarks"

7. OUTPUT:
   Valid JSON only — no markdown, no explanation, no code fences.
   Do NOT fabricate, round, convert, or infer any value.

Perform the task as per above instructions on the following laboratory report:
