# ---------------------------------------------------------------------------
# ADE – Agentic Document Extraction
# Python dependencies
#
# Paid / cloud API packages (openai SDK, together) have been replaced:
#   • LLM inference  → Ollama local server (openai SDK used as HTTP client only)
#   • Embeddings     → sentence-transformers (local, CPU/GPU)
# ---------------------------------------------------------------------------

# ── Core HTTP / async ──────────────────────────────────────────────────────
aiohappyeyeballs==2.4.4
aiohttp==3.11.10
aiosignal==1.3.1
annotated-types==0.7.0
anyio==4.6.2.post1
async-timeout==4.0.3
attrs==24.2.0
certifi==2024.8.30
charset-normalizer==3.4.0
httpcore==1.0.6
httpx==0.27.2
idna==3.10
requests==2.32.3
urllib3==2.2.3

# ── openai Python client (used as HTTP client for Ollama's /v1 endpoint) ──
openai==1.57.0

# ── LangChain ecosystem ────────────────────────────────────────────────────
langchain==0.3.11
langchain-core==0.3.24
langchain-text-splitters==0.3.2
langchain_openai
langchain_experimental
langsmith==0.2.2

# ── Reinforcement learning (contextual bandits) ────────────────────────────
gymnasium==1.0.0
Farama-Notifications==0.0.4
vowpal_wabbit_next

# ── Local LLM embeddings (replaces openai text-embedding-ada-002) ─────────
sentence-transformers>=2.2.0
# sentence-transformers pulls: transformers, torch, huggingface-hub, etc.

# ── ML / numeric ──────────────────────────────────────────────────────────
numpy==1.26.4
pandas==2.2.3
scikit-learn
torch==2.5.1
sympy==1.13.1
mpmath==1.3.0

# ── Data / serialisation ──────────────────────────────────────────────────
pydantic==2.9.2
pydantic_core==2.23.4
orjson==3.10.12
PyYAML==6.0.2
openpyxl==3.1.5
SQLAlchemy==2.0.36

# ── PDF / document processing ─────────────────────────────────────────────
pdfplumber
PyPDF2
PyMuPDF
python-docx
docx2txt

# ── Image processing / OCR ────────────────────────────────────────────────
Pillow
opencv-python
paddleocr
paddlepaddle

# ── Templating ────────────────────────────────────────────────────────────
Jinja2==3.1.4
MarkupSafe==3.0.2

# ── Utilities ─────────────────────────────────────────────────────────────
python-dotenv>=0.19.0
tqdm==4.67.0
tenacity==9.0.0
cloudpickle==3.1.0
filelock==3.16.1
fsspec==2024.10.0
packaging==24.2
six==1.17.0
pytz==2024.2
tzdata==2024.2
python-dateutil==2.9.0.post0
typing==3.7.4.3
typing_extensions==4.12.2
distro==1.9.0
colorama==0.4.6
wget==3.2

# ── Address / text parsing ────────────────────────────────────────────────
pyap==0.3.1
usaddress==0.5.11
probableparsing==0.0.1
python-crfsuite==0.9.11

# ── Web / GUI ─────────────────────────────────────────────────────────────
gradio>=4.0.0
flask
flask-socketio
werkzeug
eventlet

# ── Jupyter / notebooks ───────────────────────────────────────────────────
ipython
jupyter

# ── Pre-commit hooks ──────────────────────────────────────────────────────
pre_commit==4.0.1
cfgv==3.4.0
identify==2.6.4
nodeenv==1.9.1
virtualenv==20.28.0
